spring.liquibase.contexts: development

springdoc.swagger-ui.enabled: true

logging.level:
  org:
    springframework:
      security:
        web: DEBUG
      session:
        events: DEBUG
      web:
        servlet: ERROR
        cors: TRACE

  root: error
