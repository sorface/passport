# local.docker
server:
  port: 8080

spring.liquibase.contexts: production

springdoc.swagger-ui.enabled: true

logging.level:
  org:
    springframework:
      security:
        web: DEBUG
      session:
        events: DEBUG
      web:
        servlet: ERROR
        cors: TRACE

  root: error
